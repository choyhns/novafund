<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.member">

	<insert id="insertData" parameterType="memberDTO">
		insert into member(userId,pwd,nickname,email,
		cashPoint) values(#{userId},#{pwd},#{nickname},#{email},0)
	</insert>

	<select id="findById" parameterType="String" resultType="memberDTO">
		select userId,pwd,name,nickname,email,phone,resNum,corNum,
		address,cashPoint,enabled,tempPwd from member where userId=#{userId}
	</select>
	
	<select id="findByEmail" parameterType="String" resultType="memberDTO">
		select userId,pwd,name,nickname,email,phone,resNum,corNum,
		address,cashPoint,enabled,tempPwd from member where email=#{email}
	</select>
	
	<select id="getLists" resultType="memberDTO">
		select userId,pwd,name,nickname,email,phone,resNum,corNum,
		address,cashPoint,enabled,tempPwd from member
	</select>
	
	<update id="updateData" parameterType="memberDTO">
		update member set pwd=#{pwd},nickname=#{nickname},email=#{email},
		phone=#{phone,jdbcType=VARCHAR},address=#{address,jdbcType=VARCHAR},cashPoint=#{cashPoint,jdbcType=VARCHAR},corNum=#{corNum,jdbcType=VARCHAR}
		 where userId=#{userId}
	</update>
	
	<update id="updatePwd" parameterType="memberDTO">
		update member set pwd=#{pwd},tempPwd=#{tempPwd} where userId=#{userId}
	</update>
	
	<update id="updateEnabled" parameterType="memberDTO">
		update member set enabled=#{enabled} where userId=#{userId}
	</update>
	
	<update id="updateNickname" parameterType="memberDTO">
		update member set nickname=#{nickname} where userId=#{userId}
	</update>
	
	<update id="updateEmail" parameterType="memberDTO">
		update member set email=#{email},userId=#{email} where userId=#{userId}
	</update>
	
	<update id="updateSocialEmail" parameterType="memberDTO">
		update member set email=#{email} where userId=#{userId}
	</update>
	
	<update id="updatePhone" parameterType="memberDTO">
		update member set phone=#{phone} where userId=#{userId}
	</update>
	
	<update id="updateAddress" parameterType="memberDTO">
		update member set address=#{address} where userId=#{userId}
	</update>

	
	
</mapper>